# guides/asora

Stub folder for watchlist game `asora`.

Fill `aliases.json` and `characters/*.json` only from verified sources (GameKee 图鉴/测评, Bahamut). Mark incomplete cards with `"stub": true`. Never invent skill names or numbers.

