# Copy to .env locally — never commit .env
GAME_INTEL_DATA=./data
# JEV_BIN=jev
# JEV_PATH_PREFIX=
# GAME_INTEL_AI_URL=https://game-intel-ai.dyk1454683243.workers.dev
# Claims write key is NOT this file. From ai-site/:
#   npx wrangler secret put CLAIMS_API_KEY
# Local wrangler dev may use ai-site/.dev.vars (gitignored):
#   CLAIMS_API_KEY=replace-with-a-local-placeholder
